# Python
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/
.venv/
venv/
env/
*.egg-info/

# Node
node_modules/
web/dist/
web/.vite/
npm-debug.log*
*.tsbuildinfo
web/vite.config.js
web/vite.config.d.ts

# Env / secrets
.env
.env.*
!.env.example
!api/env.example

# Uploads (user content, never committed)
api/uploads/

# ML artifacts (large binaries, regenerate via ml/ scripts)
ml/models/*.joblib
ml/models/*.pkl
ml/data/*.csv

# Local-machine-specific compose overrides (e.g. remapping a port that
# conflicts with something else already running on this machine)
docker-compose.override.yml

# OS / editor
.DS_Store
*.log
.vscode/
.idea/
.claude/

# Coverage
.coverage
htmlcov/
coverage.xml
